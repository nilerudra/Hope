import domain from "./domain";

const apiGeneral = {
  login: `${domain}/login`,
  signup: `${domain}/signup`,
  chats: `${domain}/messages/sender/`,
  send: `${domain}/messages/send`,
};

export { apiGeneral };
